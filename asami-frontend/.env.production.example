# Same-origin production deployment behind Nginx
VITE_API_BASE_URL=/api
VITE_WS_BASE_URL=/realtime

# Access protection is handled by the Nginx container at runtime.
# Set ASAMI_FRONTEND_PASSWORD in the frontend container environment.
# Do not prefix it with VITE_: the password must never be bundled into React.
