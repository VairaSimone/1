const { pool } = require("../db/pool");
const { env } = require("../config/env");
const { buildDecisionContext, makeDecision } = require("./decision-service");
const { getEntity } = require("../repositories/entity-repo");
const { recallContext } = require("../services/memory-service");
const { buildSocialContext, deriveSocialIntent } = require("./social-relationship-service");
const { shortestRoute } = require("./action-service");
const { ensureGoalPlan, advancePlanForAction, selectActiveStep } = require("./planning-service");

const lastAutonomyDecisionAt = new Map();
const EXPLORATION_LOCATION_INTEREST = { HOME: {}, PARK: { FUN:.45,SOCIAL_NEED:.25,CURIOSITY:.30 }, CAFE: { SOCIAL_NEED:.55,BELONGING:.30,FUN:.20,CURIOSITY:.15 }, SHOP: { HUNGER:.25,THIRST:.25,CURIOSITY:.10 }, LIBRARY: { CURIOSITY:.70,ACHIEVEMENT:.55 }, SCHOOL: { ACHIEVEMENT:.60,CURIOSITY:.40 }, COMMUNITY: { SOCIAL_NEED:.50,BELONGING:.55,FUN:.25 }, GYM: { FUN:.45,ACHIEVEMENT:.20 }, CLINIC: { SAFETY:.60,COMFORT:.20 }, NATURE: { CURIOSITY:.80,FUN:.35 }, WORKSHOP: { ACHIEVEMENT:.55,CURIOSITY:.45 } };
const RESOURCE_NEED_CODES = { water:"THIRST", food:"HUNGER" };

function parseJson(value,fallback={}){if(value===null||value===undefined)return fallback;if(typeof value==="object")return value;try{return JSON.parse(value);}catch{return fallback;}}
function clamp(value,min=0,max=1){const n=Number(value);if(!Number.isFinite(n))return min;return Math.max(min,Math.min(max,n));}
async function findAutonomousActors(simulationId,limit=100){const [rows]=await pool.query(`SELECT BIN_TO_UUID(e.id) AS id FROM entities e JOIN entity_types et ON et.id=e.entity_type_id WHERE e.simulation_id=UUID_TO_BIN(?) AND et.category='ACTOR' AND e.status NOT IN ('INACTIVE','DEAD') AND NOT EXISTS (SELECT 1 FROM autonomy_policies ap WHERE ap.simulation_id=e.simulation_id AND ap.policy_type='AUTONOMY' AND ap.enabled=0 AND (ap.entity_id=e.id OR ap.entity_id IS NULL)) ORDER BY e.created_simulation_at LIMIT ?`,[simulationId,limit]);return rows.map(row=>row.id);}
function serializeReason(reason){if(reason===null||reason===undefined)return null;if(typeof reason==="string")return JSON.stringify({text:reason});return JSON.stringify(reason);}
function getGeminiTrigger(entity,context,memories=[]){if(!entity||entity.entityType!=="PERSON")return null;const candidates=context.candidates||[],top=Number(candidates[0]?.score||0),second=Number(candidates[1]?.score||0);const recentOutcomes=context.recentOutcomes||[];const recentFailure=recentOutcomes.slice(0,4).find(item=>["FAILURE","PARTIAL"].includes(String(item.outcome||"").toUpperCase()));if(recentFailure)return{reason:"recent action failure or partial outcome",priority:"HIGH",action:recentFailure.actionType,outcome:recentFailure.outcome};const failureMemory=memories.find(memory=>{const metadata=parseJson(memory.metadata,null);return metadata?.outcome==="FAILURE"||metadata?.kind==="resource_failure";});if(failureMemory)return{reason:"recent failure memory requires reflection",priority:"HIGH",memoryId:failureMemory.id||null};if(!candidates.length)return{reason:"deterministic engine has no viable candidate",priority:"HIGH"};if(top-second<.12)return{reason:"decision is ambiguous",priority:"HIGH",margin:top-second};const activeGoal=(context.goals||[]).find(goal=>Number(goal.priority||0)>=.8&&Number(goal.progress||0)<1);if(activeGoal&&context.activePlanStep&&(Number(context.activePlanStep.sequence)>1||context.activePlanStep.status==="ACTIVE"))return{reason:"active multi-step goal benefits from strategic planning",priority:"HIGH",goalId:activeGoal.id,planStepId:context.activePlanStep.id};const mentalState=context.cognitiveProfile?.mentalState||{};if(Number(mentalState.rumination||0)>=.65||Number(mentalState.certainty||1)<=.3)return{reason:"high rumination or low certainty warrants reflection",priority:"HIGH"};return null;}
function shouldAskGemini(entity,context,memories=[]){return Boolean(getGeminiTrigger(entity,context,memories));}
function canUseGeminiDecision(entityId,simulationTime,{highValue=false}={}){const now=new Date(simulationTime).getTime();if(!Number.isFinite(now))return false;const previous=lastAutonomyDecisionAt.get(entityId);if(previous===undefined){lastAutonomyDecisionAt.set(entityId,now);return true;}const configured=Number(env.GEMINI_AUTONOMY_MIN_INTERVAL_MINUTES),normalInterval=Math.max(360,Number.isFinite(configured)?configured:360),intervalMinutes=highValue?Math.min(90,normalInterval):normalInterval;if(now-previous<intervalMinutes*60000)return false;lastAutonomyDecisionAt.set(entityId,now);return true;}
async function loadWorldLocations(simulationId){const [rows]=await pool.query(`SELECT BIN_TO_UUID(e.id) AS locationId,l.location_type AS locationType,l.latitude,l.longitude,l.address_data AS addressData,e.attributes FROM locations l JOIN entities e ON e.id=l.entity_id WHERE l.simulation_id=UUID_TO_BIN(?) AND e.simulation_id=UUID_TO_BIN(?) AND e.status='ACTIVE'`,[simulationId,simulationId]);return rows.map(row=>{const attributes=parseJson(row.attributes,{});return{locationId:row.locationId,locationType:row.locationType,latitude:Number(row.latitude),longitude:Number(row.longitude),data:parseJson(row.addressData,{}),resources:attributes.resources&&typeof attributes.resources==="object"?attributes.resources:{}};});}
async function loadVisitedLocations(simulationId,entityId){const [rows]=await pool.query(`SELECT BIN_TO_UUID(location_id) AS locationId,MAX(entered_simulation_at) AS lastVisitedAt FROM entity_location_history WHERE simulation_id=UUID_TO_BIN(?) AND entity_id=UUID_TO_BIN(?) GROUP BY location_id`,[simulationId,entityId]);return new Map(rows.map(row=>[row.locationId,row.lastVisitedAt]));}
function locationInterestScore(location,needs){const weights=EXPLORATION_LOCATION_INTEREST[location.locationType]||{};return Object.entries(weights).reduce((sum,[needCode,weight])=>sum+clamp(needs.find(item=>item.code===needCode)?.value||0)*weight,0);}
function explorationNoveltyScore(lastVisitedAt,simulationTime){if(!lastVisitedAt)return 1;const elapsed=(new Date(simulationTime).getTime()-new Date(lastVisitedAt).getTime())/3600000;if(!Number.isFinite(elapsed))return .5;if(elapsed>=72)return .95;if(elapsed>=24)return .75;if(elapsed>=8)return .50;if(elapsed>=2)return .30;return .10;}
function routeTravelMinutes(route){if(!route||!Number.isFinite(Number(route.distanceMeters)))return Infinity;return Number(route.distanceMeters)/1000/4.8*60;}
async function chooseExplorationDestination(simulationId,entityId,originId,needs,simulationTime){if(!originId)return null;const [locations,visited]=await Promise.all([loadWorldLocations(simulationId),loadVisitedLocations(simulationId,entityId)]);const origin=locations.find(location=>location.locationId===originId);if(!origin)return null;const candidates=[];for(const location of locations){if(location.locationId===originId)continue;const route=shortestRoute(locations,originId,location.locationId);if(!route)continue;const travelMinutes=routeTravelMinutes(route);if(!Number.isFinite(travelMinutes))continue;const novelty=explorationNoveltyScore(visited.get(location.locationId),simulationTime),interest=locationInterestScore(location,needs);let resourceOpportunity=0;for(const [resource,needCode] of Object.entries(RESOURCE_NEED_CODES)){const amount=Number(location.resources?.[resource]||0),pressure=Number(needs.find(item=>item.code===needCode)?.value||0);if(amount>=1)resourceOpportunity+=Math.min(.35,pressure*.35);}const distancePenalty=Math.min(.60,travelMinutes/60*.60),score=novelty*1.35+interest*1.15+resourceOpportunity-distancePenalty+Math.random()*.05;candidates.push({locationId:location.locationId,locationType:location.locationType,travelMinutes,distanceMeters:route.distanceMeters,score,novelty,interest,resourceOpportunity});}candidates.sort((a,b)=>b.score-a.score);return candidates[0]||null;}
async function getEntityLocation(simulationId,entityId){const [rows]=await pool.query(`SELECT BIN_TO_UUID(location_id) AS locationId FROM entity_locations_current WHERE simulation_id=UUID_TO_BIN(?) AND entity_id=UUID_TO_BIN(?) LIMIT 1`,[simulationId,entityId]);return rows[0]?.locationId||null;}
async function chooseRemoteSocialTarget(simulationId,entityId,originId){const [rows]=await pool.query(`SELECT BIN_TO_UUID(other.id) AS id,other.display_name AS name,BIN_TO_UUID(targetLoc.location_id) AS locationId,BIN_TO_UUID(r.id) AS relationshipId,COALESCE(r.familiarity_score,0) AS familiarity,COALESCE(r.closeness_score,0) AS closeness,COALESCE(r.affection_score,0) AS affection,COALESCE(r.trust_score,0) AS trust,COALESCE(r.attraction_score,0) AS attraction FROM entities other JOIN persons p ON p.entity_id=other.id JOIN entity_locations_current targetLoc ON targetLoc.entity_id=other.id AND targetLoc.simulation_id=UUID_TO_BIN(?) LEFT JOIN relationships r ON r.simulation_id=UUID_TO_BIN(?) AND r.status='ACTIVE' AND ((r.source_entity_id=UUID_TO_BIN(?) AND r.target_entity_id=other.id) OR (r.source_entity_id=other.id AND r.target_entity_id=UUID_TO_BIN(?))) WHERE other.simulation_id=UUID_TO_BIN(?) AND other.status='ACTIVE' AND other.id<>UUID_TO_BIN(?) ORDER BY other.display_name LIMIT 50`,[simulationId,simulationId,entityId,entityId,simulationId,entityId]);if(!rows.length)return null;const locations=await loadWorldLocations(simulationId),candidates=[];for(const row of rows){if(!row.locationId||row.locationId===originId)continue;const route=shortestRoute(locations,originId,row.locationId);if(!route)continue;const travelMinutes=routeTravelMinutes(route);if(!Number.isFinite(travelMinutes))continue;const relationshipScore=Number(row.familiarity)*.9+Number(row.closeness)*1.1+Number(row.affection)*1.2+Number(row.trust)+Number(row.attraction)*.8,noveltyBonus=row.relationshipId?0:.30,distancePenalty=Math.min(.70,travelMinutes/60*.70);candidates.push({entityId:row.id,name:row.name,locationId:row.locationId,travelMinutes,distanceMeters:route.distanceMeters,score:relationshipScore+noveltyBonus-distancePenalty,relationshipId:row.relationshipId});}candidates.sort((a,b)=>b.score-a.score||a.travelMinutes-b.travelMinutes);return candidates[0]||null;}
async function chooseSocialTarget(socialContext,simulationId,entityId,originId){const candidates=Array.isArray(socialContext?.candidates)?socialContext.candidates:[];if(candidates.length){const ranked=candidates.slice().sort((a,b)=>{const aPartner=socialContext.partner?.partnerId===a.id?1:0,bPartner=socialContext.partner?.partnerId===b.id?1:0,aScore=Number(a.score||0)+aPartner*.35+Number(a.romanticScore||0)*.35,bScore=Number(b.score||0)+bPartner*.35+Number(b.romanticScore||0)*.35;return bScore-aScore;});const target=ranked[0];return{entityId:target.id,name:target.name,locationId:originId,remote:false,score:Number(target.score||0)};}return chooseRemoteSocialTarget(simulationId,entityId,originId);}
function sanitizeGeminiChoice(aiChoice,context,{socialContext,currentLocationId,worldLocations=[]}={}){if(!aiChoice)return null;const allowed=new Set((context.allowedActionTypes||[]).map(value=>String(value).toUpperCase())),selectedAction=String(aiChoice.selectedActionType||"").toUpperCase();if(!allowed.has(selectedAction))return null;const knownEntityIds=new Set((socialContext?.candidates||[]).map(candidate=>candidate.id));const targetEntityId=aiChoice.targetEntityId&&knownEntityIds.has(aiChoice.targetEntityId)?aiChoice.targetEntityId:null;let targetLocationId=null;if(aiChoice.targetLocationId){const location=worldLocations.find(item=>item.locationId===aiChoice.targetLocationId);const route=currentLocationId&&location?shortestRoute(worldLocations,currentLocationId,aiChoice.targetLocationId):null;if(location&&route)targetLocationId=aiChoice.targetLocationId;}const strategy=aiChoice.strategy&&typeof aiChoice.strategy==="object"?aiChoice.strategy:null;const proposal=aiChoice.planProposal&&typeof aiChoice.planProposal==="object"?{...aiChoice.planProposal,steps:(aiChoice.planProposal.steps||[]).filter(step=>!step.actionType||allowed.has(String(step.actionType).toUpperCase())).map(step=>({...step,actionType:step.actionType?String(step.actionType).toUpperCase():undefined})).slice(0,8)}:null;return{...aiChoice,selectedActionType:selectedAction,targetEntityId,targetLocationId,strategy,planProposal:proposal};}

async function actForEntity({simulationId,entityId,simulationTime,gemini}){const entity=await getEntity(simulationId,entityId);if(!entity)return null;let context=await buildDecisionContext(simulationId,entityId,simulationTime);const goalState=await ensureGoalPlan({simulationId,entityId,simulationTime,needs:context.needs});if(goalState.created||(goalState.goal&&!context.goals.some(goal=>goal.id===goalState.goal.id)))context=await buildDecisionContext(simulationId,entityId,simulationTime);const socialContext=entity.entityType==="PERSON"?await buildSocialContext(simulationId,entityId):{partner:null,candidates:[]};context.social={partner:socialContext.partner,candidates:socialContext.candidates.map(candidate=>({id:candidate.id,name:candidate.name,relationshipType:candidate.relationshipType,compatibility:Number(candidate.compatibility.toFixed(3)),romanticScore:Number(candidate.romanticScore.toFixed(3)),familiarity:Number(candidate.relationship?.familiarity||0),closeness:Number(candidate.relationship?.closeness||0),affection:Number(candidate.relationship?.affection||0),trust:Number(candidate.relationship?.trust||0)}))};const currentLocationId=context.location?.locationId||await getEntityLocation(simulationId,entityId);let socialTarget=null;if(entity.entityType==="PERSON"&&currentLocationId){socialTarget=await chooseSocialTarget(socialContext,simulationId,entityId,currentLocationId);if(socialTarget)context.social.travelTarget={entityId:socialTarget.entityId,name:socialTarget.name,locationId:socialTarget.locationId,remote:Boolean(socialTarget.remote),travelMinutes:Number(socialTarget.travelMinutes||0)};}const explorationDestination=entity.entityType==="PERSON"&&currentLocationId?await chooseExplorationDestination(simulationId,entityId,currentLocationId,context.needs,simulationTime):null;if(explorationDestination)context.explorationDestination=explorationDestination;const plan=goalState.goal?goalState.plan||(await ensureGoalPlan({simulationId,entityId,simulationTime,needs:context.needs})).plan:null,activePlanStep=selectActiveStep(plan);if(activePlanStep)context.activePlanStep=activePlanStep;const memories=await recallContext(simulationId,entityId,8),geminiTrigger=getGeminiTrigger(entity,context,memories);context.geminiTrigger=geminiTrigger;let aiChoice=null;if(gemini?.client&&geminiTrigger&&canUseGeminiDecision(entity.id,simulationTime,{highValue:geminiTrigger.priority==="HIGH"})){const worldLocations=await loadWorldLocations(simulationId);aiChoice=sanitizeGeminiChoice(await gemini.chooseDecision({entity:{id:entity.id,name:entity.displayName},needs:context.needs,traits:context.traits,goals:context.goals,memories,allowedActionTypes:context.allowedActionTypes,candidates:context.candidates,location:context.location,social:context.social,explorationDestination,activePlanStep,trigger:geminiTrigger}),context,{socialContext,currentLocationId,worldLocations});}const decision=await makeDecision({simulationId,entityId,simulationTime,context,aiChoice});if(decision.actionType==="EXPLORING"&&explorationDestination){decision.targetLocationId=aiChoice?.targetLocationId||explorationDestination.locationId;decision.explorationDestination=explorationDestination;}if(decision.actionType==="TALKING"){const aiTarget=aiChoice?.targetEntityId,localAiTarget=socialContext.candidates.some(candidate=>candidate.id===aiTarget);socialTarget=localAiTarget?await chooseSocialTarget(socialContext,simulationId,entityId,currentLocationId):socialTarget;if(socialTarget){decision.targetEntityId=socialTarget.entityId;if(socialTarget.locationId&&socialTarget.locationId!==currentLocationId){decision.actionType="WALKING";decision.targetLocationId=socialTarget.locationId;decision.socialTravel={targetEntityId:socialTarget.entityId,targetName:socialTarget.name,destinationLocationId:socialTarget.locationId,expectedTravelMinutes:Number(socialTarget.travelMinutes||0)};decision.relationshipIntent="NONE";}}}if(decision.actionType==="TALKING")decision.relationshipIntent=deriveSocialIntent({actionType:decision.actionType,targetId:decision.targetEntityId,partner:socialContext.partner,candidates:socialContext.candidates});await pool.query(`UPDATE decisions SET context=? WHERE id=UUID_TO_BIN(?)`,[JSON.stringify({...context,aiChoice:aiChoice||null,chosenAction:decision.actionType,relationshipDecision:{intent:decision.relationshipIntent||"NONE",targetEntityId:decision.targetEntityId||null},targetLocationId:decision.targetLocationId||null,activePlanStep:activePlanStep||null}),decision.decisionId]);const intentionId=require("../lib/ids").uuid(),selectedCandidate=context.candidates.find(candidate=>candidate.action===decision.actionType);await pool.query(`INSERT INTO intentions(id,simulation_id,entity_id,goal_id,action_type,target_entity_id,scheduled_simulation_at,priority,status,reason,created_simulation_at,version) VALUES(UUID_TO_BIN(?),UUID_TO_BIN(?),UUID_TO_BIN(?),UUID_TO_BIN(?),?,UUID_TO_BIN(?),?,?,'ACTIVE',?,?,1)`,[intentionId,simulationId,entityId,goalState.goal?.id||null,decision.actionType,decision.targetEntityId||null,simulationTime,Number(selectedCandidate?.score||0),serializeReason(decision.reason),simulationTime]);if(decision.targetLocationId||decision.socialTravel||decision.explorationDestination)await pool.query(`UPDATE decision_options SET option_code=?,description=?,action_definition=?,evaluation=?,expected_outcome=? WHERE id=(SELECT selected_option_id FROM decisions WHERE id=UUID_TO_BIN(?) LIMIT 1)`,[decision.actionType,`Autonomously selected ${decision.actionType}`,JSON.stringify({actionType:decision.actionType,targetEntityId:decision.targetEntityId||null,targetLocationId:decision.targetLocationId||null,strategy:decision.strategy||null}),JSON.stringify({score:Number(selectedCandidate?.score||0),targetLocationId:decision.targetLocationId||null,aiAccepted:Boolean(decision.aiAccepted)}),JSON.stringify({actionType:decision.actionType,targetLocationId:decision.targetLocationId||null,strategy:decision.strategy||null,planProposal:decision.planProposal||null}),decision.decisionId]);decision.intentionId=intentionId;decision.goalId=goalState.goal?.id||null;decision.planId=plan?.id||null;decision.planStepId=activePlanStep?.id||null;return decision;}
function goalActionSatisfiesNeed(needCode,actionType){const mapping={HUNGER:new Set(["EATING"]),THIRST:new Set(["DRINKING"]),SLEEPINESS:new Set(["SLEEPING"]),ENERGY:new Set(["SLEEPING","RESTING"]),SOCIAL_NEED:new Set(["TALKING"]),BELONGING:new Set(["TALKING"]),FUN:new Set(["PLAYING","WATCHING"]),CURIOSITY:new Set(["STUDYING","READING","EXPLORING"]),ACHIEVEMENT:new Set(["STUDYING","READING","WORKING"])};return mapping[String(needCode||"").toUpperCase()]?.has(String(actionType||"").toUpperCase())||false;}
async function completeGoalForAction(goalId,actionType,simulationTime,outcome="SUCCESS",actionResult=null){if(!goalId)return{changed:false,completed:false,progress:null,planId:null};const [rows]=await pool.query(`SELECT BIN_TO_UUID(simulation_id) AS simulationId,BIN_TO_UUID(entity_id) AS entityId FROM goals WHERE id=UUID_TO_BIN(?) LIMIT 1`,[goalId]);if(!rows.length)return{changed:false,completed:false,progress:null,planId:null};return advancePlanForAction({simulationId:rows[0].simulationId,entityId:rows[0].entityId,goalId,actionType,outcome,simulationTime,actionResult});}
module.exports={findAutonomousActors,actForEntity,completeGoalForAction,shouldAskGemini,canUseGeminiDecision,goalActionSatisfiesNeed,chooseSocialTarget,chooseExplorationDestination,getGeminiTrigger,sanitizeGeminiChoice};