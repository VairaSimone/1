const { pool } = require("../db/pool");

const LOCATION_ENTITY_TYPE_ID = "00000000-0000-4000-8000-000000000003";

const LOCATION_RESOURCES = {
  HOME: { water: 24, food: 14, beds: 1, books: 4 }, PARK: { water: 30, food: 0, beds: 0, books: 0 },
  CAFE: { water: 80, food: 120, beds: 0, books: 8 }, GROCERY: { water: 160, food: 240, beds: 0, books: 0 },
  LIBRARY: { water: 24, food: 0, beds: 180 }, SQUARE: { water: 20, food: 8, beds: 0, books: 0 },
  SCHOOL: { water: 40, food: 20, beds: 0, books: 80 }, COMMUNITY: { water: 35, food: 30, beds: 0, books: 30 },
  GYM: { water: 70, food: 10, beds: 0, books: 0 }, CLINIC: { water: 80, food: 10, beds: 1, books: 15 },
  NATURE: { water: 18, food: 0, beds: 0, books: 0 }, WORKSHOP: { water: 24, food: 8, beds: 0, books: 12 }
};

const LOCATION_OBJECTS = {
  HOME:["bed","refrigerator","table","bookshelf"], PARK:["bench","fountain","pond"],
  CAFE:["counter","tables","chairs","bookshelf","coffee_machine"], GROCERY:["shelves","checkout","refrigerated_case","produce_section"],
  LIBRARY:["bookshelves","reading_tables","chairs","water_fountain"], SQUARE:["benches","fountain","street_lamps"],
  SCHOOL:["classrooms","desks","library_shelves","water_fountain"], COMMUNITY:["meeting_room","chairs","kitchen","storage"],
  GYM:["treadmills","weights","lockers","water_fountain"], CLINIC:["reception","exam_room","beds","water_station"],
  NATURE:["trail","pond","benches","signposts"], WORKSHOP:["workbenches","tools","storage","safety_sink"]
};

function parseJson(value,fallback={}){if(value===null||value===undefined)return fallback;if(typeof value==='object')return value;try{return JSON.parse(value);}catch{return fallback;}}
function clamp(value,min=0,max=Number.POSITIVE_INFINITY){return Math.max(min,Math.min(max,Number(value)||0));}
async function locationRow(simulationId,locationId,db=pool){const [rows]=await db.query(`SELECT BIN_TO_UUID(e.id) AS locationId,e.attributes,e.version,l.location_type AS locationType FROM entities e JOIN locations l ON l.entity_id=e.id AND l.simulation_id=e.simulation_id WHERE e.simulation_id=UUID_TO_BIN(?) AND e.id=UUID_TO_BIN(?) AND e.entity_type_id=UUID_TO_BIN(?) AND e.status='ACTIVE' LIMIT 1${db===pool?'':' FOR UPDATE'}`,[simulationId,locationId,LOCATION_ENTITY_TYPE_ID]);return rows[0]||null;}
async function updateLocationAttributes(simulationId,locationId,updater,db=pool){for(let attempt=0;attempt<3;attempt++){const row=await locationRow(simulationId,locationId,db);if(!row)return null;const next=updater(parseJson(row.attributes,{}));const[updated]=await db.query(`UPDATE entities SET attributes=?,version=version+1 WHERE id=UUID_TO_BIN(?) AND simulation_id=UUID_TO_BIN(?) AND version=?`,[JSON.stringify(next),locationId,simulationId,Number(row.version||1)]);if(updated.affectedRows)return next;}return null;}
async function seedPhysicalWorld(simulationId,simulationTime){const[rows]=await pool.query(`SELECT BIN_TO_UUID(e.id) AS locationId,e.attributes,e.version,l.location_type AS locationType FROM entities e JOIN locations l ON l.entity_id=e.id AND l.simulation_id=e.simulation_id WHERE e.simulation_id=UUID_TO_BIN(?) AND e.entity_type_id=UUID_TO_BIN(?) AND e.status='ACTIVE'`,[simulationId,LOCATION_ENTITY_TYPE_ID]);for(const row of rows){const attributes=parseJson(row.attributes,{}),code=attributes.worldCode||row.locationType;if(!LOCATION_RESOURCES[code])continue;const resources=attributes.resources||{},objects=Array.isArray(attributes.objects)?attributes.objects:[],defaults=LOCATION_RESOURCES[code],desiredResources=Object.fromEntries(Object.entries(defaults).map(([k,v])=>[k,Number.isFinite(Number(resources[k]))?Number(resources[k]):v])),desiredObjects=objects.length?objects:(LOCATION_OBJECTS[code]||[]),next={...attributes,resources:desiredResources,objects:desiredObjects,physicalUpdatedAt:simulationTime};if(JSON.stringify(resources)===JSON.stringify(desiredResources)&&JSON.stringify(objects)===JSON.stringify(desiredObjects))continue;await pool.query(`UPDATE entities SET attributes=?,version=version+1 WHERE id=UUID_TO_BIN(?) AND simulation_id=UUID_TO_BIN(?) AND version=?`,[JSON.stringify(next),row.locationId,simulationId,Number(row.version||1)]);}}
async function getLocationPhysicalState(simulationId,locationId){const row=await locationRow(simulationId,locationId);if(!row)return null;const a=parseJson(row.attributes,{});return{locationId,locationType:row.locationType,resources:a.resources||{},objects:Array.isArray(a.objects)?a.objects:[]};}
async function consumeResource({simulationId,locationId,resource,amount,simulationTime,db=pool}){const quantity=Math.max(0,Number(amount)||0);if(!locationId||!resource||!quantity)return{ok:true,consumed:0,remaining:null,resource:resource||null};let result=null;const next=await updateLocationAttributes(simulationId,locationId,current=>{const resources={...(current.resources||{})},available=clamp(resources[resource],0),consumed=Math.min(available,quantity);resources[resource]=available-consumed;result={ok:consumed>=quantity,consumed,remaining:resources[resource],resource};return{...current,resources,physicalUpdatedAt:simulationTime};},db);return next&&result?result:{ok:false,consumed:0,remaining:null,resource};}
async function replenishResource({simulationId,locationId,resource,amount,simulationTime}){const quantity=Math.max(0,Number(amount)||0);if(!locationId||!resource||!quantity)return null;let remaining=null;const next=await updateLocationAttributes(simulationId,locationId,current=>{const resources={...(current.resources||{})};remaining=clamp(resources[resource],0)+quantity;resources[resource]=remaining;return{...current,resources,physicalUpdatedAt:simulationTime};});return next?remaining:null;}
async function resolveActionResource({simulationId,locationId,actionType,simulationTime,conn=null}){const usage={DRINKING:{resource:'water',amount:1},EATING:{resource:'food',amount:1}}[actionType];if(!usage)return{ok:true,consumed:0,remaining:null,resource:null};return consumeResource({simulationId,locationId,resource:usage.resource,amount:usage.amount,simulationTime,db:conn||pool});}
module.exports={seedPhysicalWorld,getLocationPhysicalState,consumeResource,replenishResource,resolveActionResource,LOCATION_RESOURCES,LOCATION_OBJECTS};