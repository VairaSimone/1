const { pool } = require("../db/pool");
const { uuid } = require("../lib/ids");
const { clamp } = require("./state-rules");

function round5(value) { return Math.round((Number(value) + Number.EPSILON) * 100000) / 100000; }
function parseJson(value, fallback = {}) { if (value === null || value === undefined) return fallback; if (typeof value === "object") return value; try { return JSON.parse(value); } catch { return fallback; } }

const PRESSURE_NEEDS = new Set(["HUNGER", "THIRST", "SLEEPINESS", "SOCIAL_NEED", "FUN", "CURIOSITY", "ACHIEVEMENT", "BELONGING"]);
const ACTION_PRESSURE_FLOORS = { TALKING:{SOCIAL_NEED:0.18,BELONGING:0.15},PLAYING:{FUN:0.18,SOCIAL_NEED:0.10},STUDYING:{ACHIEVEMENT:0.15,CURIOSITY:0.15},READING:{CURIOSITY:0.15,ACHIEVEMENT:0.12},EXPLORING:{CURIOSITY:0.15},WORKING:{ACHIEVEMENT:0.15},WALKING:{FUN:0.15,CURIOSITY:0.10},WATCHING:{FUN:0.15} };

/* Action effects are rates, not hard resets. Pressure relief and restorative gains both have diminishing returns. */
const ACTION_NEED_GAINS = {
  SLEEPING:{SLEEPINESS:-0.65,ENERGY:0.16,COMFORT:0.22,SAFETY:0.02},
  EATING:{HUNGER:-1.45,ENERGY:0.07,COMFORT:0.10,SAFETY:0.01},
  DRINKING:{THIRST:-2.15,ENERGY:0.04,SAFETY:0.01},
  TALKING:{SOCIAL_NEED:-0.45,BELONGING:-0.18},PLAYING:{FUN:-0.50,SOCIAL_NEED:-0.12,COMFORT:0.04},
  RESTING:{ENERGY:0.22,COMFORT:0.28,SLEEPINESS:-0.12},STUDYING:{ACHIEVEMENT:-0.80,CURIOSITY:-0.50,ENERGY:-0.15,FUN:-0.10,COMFORT:-0.03},
  READING:{CURIOSITY:-0.40,ACHIEVEMENT:-0.30,FUN:0.10,COMFORT:0.03},EXPLORING:{CURIOSITY:-1.20,FUN:-0.50,ENERGY:-0.15,COMFORT:-0.05,SAFETY:-0.02},
  WALKING:{FUN:-0.25,ENERGY:-0.08,COMFORT:0.02,SAFETY:0.01},WORKING:{ACHIEVEMENT:-0.70,ENERGY:-0.20,FUN:-0.10,COMFORT:-0.04,SAFETY:0.005},
  WATCHING:{FUN:-1.10,ENERGY:0.05,COMFORT:0.06,SAFETY:0.02},SCHOOL:{ACHIEVEMENT:-0.60,CURIOSITY:-0.40,ENERGY:-0.12,FUN:-0.05,COMFORT:-0.03,SAFETY:0.01}
};

/* Relative metabolic/activity load. Sleep and rest consume far less hunger/thirst/energy than active behavior. */
const ACTION_DECAY_MULTIPLIERS = {
  SLEEPING:{HUNGER:0.30,THIRST:0.28,SLEEPINESS:0.45,ENERGY:0.05,SOCIAL_NEED:0.75,FUN:0.70,CURIOSITY:0.65,ACHIEVEMENT:0.70,BELONGING:0.75},
  RESTING:{HUNGER:0.55,THIRST:0.50,SLEEPINESS:0.70,ENERGY:0.35,SOCIAL_NEED:0.80,FUN:0.80,CURIOSITY:0.75,ACHIEVEMENT:0.75,BELONGING:0.80},
  DRINKING:{HUNGER:0.85,THIRST:0.55,SLEEPINESS:0.85,ENERGY:0.80},EATING:{HUNGER:0.65,THIRST:0.75,SLEEPINESS:0.85,ENERGY:0.75},
  TALKING:{HUNGER:1.00,THIRST:1.00,ENERGY:1.00},PLAYING:{HUNGER:1.15,THIRST:1.20,ENERGY:1.35},STUDYING:{HUNGER:1.00,THIRST:1.00,ENERGY:1.20},
  READING:{HUNGER:0.95,THIRST:0.95,ENERGY:0.90},EXPLORING:{HUNGER:1.15,THIRST:1.20,ENERGY:1.40},WALKING:{HUNGER:1.05,THIRST:1.10,ENERGY:1.12},
  WORKING:{HUNGER:1.20,THIRST:1.25,ENERGY:1.45},WATCHING:{HUNGER:0.90,THIRST:0.90,ENERGY:0.65},SCHOOL:{HUNGER:1.00,THIRST:1.00,ENERGY:1.15}
};

const ACTION_EMOTION_EFFECTS = { TALKING:{JOY:0.035,CALM:0.02,EXCITEMENT:0.012,ANXIETY:-0.008,SADNESS:-0.008},PLAYING:{JOY:0.045,EXCITEMENT:0.035,CALM:0.008,FRUSTRATION:-0.02,SADNESS:-0.01},EATING:{JOY:0.02,CALM:0.025,FRUSTRATION:-0.02,ANXIETY:-0.01},DRINKING:{CALM:0.025,JOY:0.012,ANXIETY:-0.012},SLEEPING:{CALM:0.04,JOY:0.015,ANXIETY:-0.02,FRUSTRATION:-0.02},RESTING:{CALM:0.03,JOY:0.012,ANXIETY:-0.015,FRUSTRATION:-0.015},STUDYING:{EXCITEMENT:0.012,JOY:0.012,FRUSTRATION:0.008,CALM:0.008},READING:{JOY:0.018,CALM:0.018,EXCITEMENT:0.01},EXPLORING:{EXCITEMENT:0.05,JOY:0.025,FEAR:0.018,ANXIETY:0.01},WALKING:{CALM:0.025,JOY:0.012,ANXIETY:-0.01},WORKING:{FRUSTRATION:0.012},SCHOOL:{FRUSTRATION:0.008,EXCITEMENT:0.008,ANXIETY:0.004},WATCHING:{JOY:0.022,CALM:0.015,EXCITEMENT:0.018} };
const NEED_EMOTION_CURVES = { HUNGER:{threshold:0.40,frustration:0.10,anger:0.055}, THIRST:{threshold:0.40,frustration:0.15,anxiety:0.06}, SLEEPINESS:{threshold:0.40,frustration:0.10,sadness:0.065}, SOCIAL_NEED:{threshold:0.40,sadness:0.08,anxiety:0.045,frustration:0.055}, BELONGING:{threshold:0.40,sadness:0.10,anxiety:0.06}, FUN:{threshold:0.40,sadness:0.065,frustration:0.045}, CURIOSITY:{threshold:0.40,excitement:0.075}, ACHIEVEMENT:{threshold:0.40,frustration:0.07} };
const TRAIT_BEHAVIOR_LINKS={TALKING:{EXTRAVERSION:.60,SOCIABILITY:.70,EMPATHY:.25},EXPLORING:{OPENNESS:.55,CURIOSITY:.60,CONFIDENCE:.20},STUDYING:{CONSCIENTIOUSNESS:.60,DISCIPLINE:.70,PATIENCE:.25},WORKING:{CONSCIENTIOUSNESS:.55,DISCIPLINE:.55},PLAYING:{OPENNESS:.35,IMPULSIVITY:.25},READING:{OPENNESS:.35,CURIOSITY:.55},WALKING:{OPENNESS:.20},SLEEPING:{PATIENCE:.12,SELF_CARE:.15},RESTING:{PATIENCE:.12,SELF_CARE:.15},EATING:{SELF_CARE:.20},DRINKING:{SELF_CARE:.20},WATCHING:{OPENNESS:.12}};
function pressureCurve(value,threshold=0.4){const v=clamp(value);if(v<=threshold)return 0;const normalized=(v-threshold)/(1-threshold);return normalized*normalized;}

async function ensureEntityState(entityId,simulationTime){await Promise.all([pool.query(`INSERT IGNORE INTO entity_needs_current(entity_id,need_id,value,updated_simulation_at,version) SELECT UUID_TO_BIN(?),id,default_value,?,1 FROM need_definitions WHERE active=1`,[entityId,simulationTime]),pool.query(`INSERT IGNORE INTO entity_emotions_current(entity_id,emotion_id,intensity,updated_simulation_at,version) SELECT UUID_TO_BIN(?),id,default_value,?,1 FROM emotion_definitions WHERE active=1`,[entityId,simulationTime]),pool.query(`INSERT IGNORE INTO entity_traits_current(entity_id,trait_id,value,updated_simulation_at,version) SELECT UUID_TO_BIN(?),id,default_value,?,1 FROM trait_definitions WHERE active=1`,[entityId,simulationTime]),pool.query(`INSERT IGNORE INTO entity_skills(entity_id,skill_id,updated_simulation_at,version) SELECT UUID_TO_BIN(?),id,?,1 FROM skill_definitions WHERE active=1`,[entityId,simulationTime])]);}
async function readNeeds(entityId){const [rows]=await pool.query(`SELECT BIN_TO_UUID(enc.need_id) AS needId,nd.code,nd.name,enc.value,enc.version,nd.decay_rate AS decayRate,nd.recovery_rate AS recoveryRate,nd.priority_weight AS priorityWeight,nd.parameters FROM entity_needs_current enc JOIN need_definitions nd ON nd.id=enc.need_id WHERE enc.entity_id=UUID_TO_BIN(?) AND nd.active=1`,[entityId]);return rows;}
function actionDecayMultiplier(actionType,needCode){return Number(ACTION_DECAY_MULTIPLIERS[String(actionType||"").toUpperCase()]?.[needCode] ?? 1);}
function saturatedActionDelta(actionType,needCode,currentValue,hours){const rate=Number((ACTION_NEED_GAINS[actionType]||{})[needCode]||0);if(!rate)return 0;const value=clamp(currentValue),amount=Math.abs(rate)*hours;if(rate<0)return-amount*value;return amount*(1-value);}
async function updateNeeds(entityId,simulationTime,deltaHours,causeEventId=null,causeActionId=null,activeActionType=null){const rows=await readNeeds(entityId),changes=[],hours=Math.min(Math.max(Number(deltaHours)||0,0),6),action=String(activeActionType||"").toUpperCase();for(const r of rows){const decayRate=Math.max(0,Number(r.decayRate)||0),recoveryRate=Math.max(0,Number(r.recoveryRate)||0),multiplier=actionDecayMultiplier(action,r.code);let delta;if(PRESSURE_NEEDS.has(r.code))delta=decayRate*hours*multiplier;else if(r.code==="ENERGY")delta=-decayRate*hours*multiplier;else if(r.code==="SAFETY")delta=recoveryRate*0.25*hours;else if(r.code==="COMFORT")delta=(recoveryRate*0.15-decayRate*0.05)*hours;else delta=-decayRate*hours;if(action){const rawGain=saturatedActionDelta(action,r.code,r.value,hours),floor=ACTION_PRESSURE_FLOORS[action]?.[r.code];if(PRESSURE_NEEDS.has(r.code)&&rawGain<0&&floor!==undefined)delta+=Math.max(rawGain,-Math.max(0,Number(r.value)-floor));else if(PRESSURE_NEEDS.has(r.code)&&rawGain<0)delta+=Math.max(rawGain,-Number(r.value)*0.60);else delta+=rawGain;}const oldValue=round5(r.value);let next=round5(clamp(oldValue+delta));const floor=ACTION_PRESSURE_FLOORS[action]?.[r.code];if(PRESSURE_NEEDS.has(r.code)&&floor!==undefined&&action)next=Math.max(next,floor);const historyDelta=round5(next-oldValue);if(Math.abs(historyDelta)<0.000001)continue;const [updated]=await pool.query(`UPDATE entity_needs_current SET value=?,updated_simulation_at=?,version=version+1 WHERE entity_id=UUID_TO_BIN(?) AND need_id=UUID_TO_BIN(?) AND version=?`,[next,simulationTime,entityId,r.needId,r.version]);if(!updated.affectedRows)continue;await pool.query(`INSERT INTO entity_need_history(id,entity_id,need_id,old_value,new_value,delta,simulation_time,cause_event_id,cause_action_id) VALUES(UUID_TO_BIN(?),UUID_TO_BIN(?),UUID_TO_BIN(?),?,?,?,?,UUID_TO_BIN(?),UUID_TO_BIN(?))`,[uuid(),entityId,r.needId,oldValue,next,historyDelta,simulationTime,causeEventId,causeActionId]);changes.push({code:r.code,old:oldValue,new:next,delta:historyDelta});}return changes;}
function emotionAppraisal(actionType,needs,context={}){const codes=["JOY","SADNESS","ANGER","FEAR","ANXIETY","FRUSTRATION","EXCITEMENT","CALM","DISGUST","SHAME"],deltas=Object.fromEntries(codes.map(c=>[c,0])),add=(c,v)=>{deltas[c]+=v;};if(!context.event)for(const [c,v] of Object.entries(ACTION_EMOTION_EFFECTS[actionType]||{}))add(c,v);const p=Object.fromEntries((needs||[]).map(n=>[n.code,clamp(n.new)]));for(const [code,curve] of Object.entries(NEED_EMOTION_CURVES)){const pressure=pressureCurve(p[code]||0,curve.threshold);if(curve.frustration)add("FRUSTRATION",curve.frustration*pressure);if(curve.anger)add("ANGER",curve.anger*pressure);if(curve.sadness)add("SADNESS",curve.sadness*pressure);if(curve.anxiety)add("ANXIETY",curve.anxiety*pressure);if(curve.excitement)add("EXCITEMENT",curve.excitement*pressure);}const safety=p.SAFETY??1,energy=p.ENERGY??1;if(safety<0.45){add("FEAR",(0.45-safety)*.06);add("ANXIETY",(0.45-safety)*.04);}if(energy<0.3){add("FRUSTRATION",(.3-energy)*.04);add("SADNESS",(.3-energy)*.025);}if(context.event){const outcome=String(context.outcome||"SUCCESS").toUpperCase(),expected=String(context.expectedOutcome?.outcome||context.expectedOutcome?.status||context.expectedOutcome||"").toUpperCase(),mismatch=expected&&expected!==outcome,impact=mismatch?1.25:1;if(outcome==="SUCCESS"){add("JOY",.055*impact);add("CALM",.025);add("EXCITEMENT",mismatch?.018:.008);}else if(outcome==="PARTIAL"){add("JOY",.012);add("FRUSTRATION",.045*impact);add("ANXIETY",.018);add("SADNESS",.012);}else{add("FRUSTRATION",.075*impact);add("ANXIETY",.04*impact);add("SADNESS",.025);add("ANGER",.018);if(context.failureReason==="RESOURCE_UNAVAILABLE")add("DISGUST",.008);}if(context.targetEntityId&&outcome==="SUCCESS"){add("JOY",.025);add("CALM",.01);}if(context.targetEntityId&&outcome==="FAILURE"){add("ANXIETY",.025);add("SADNESS",.02);}if(context.meaning==="GOAL_PROGRESS")add(outcome==="SUCCESS"?"JOY":"FRUSTRATION",.025);if(context.meaning==="GOAL_BLOCKED"){add("FRUSTRATION",.035);add("ANXIETY",.018);}if(Number(context.needRelief||0)>.35){add("CALM",.025);add("JOY",.018);}}return deltas;}
async function applyEmotions(entityId,simulationTime,changes,causeEventId=null,causeActionId=null,actionType=null,deltaHours=0,appraisalContext=null){const [rows]=await pool.query(`SELECT BIN_TO_UUID(eec.emotion_id) AS emotionId,ed.code,eec.intensity,eec.version,ed.decay_rate AS decayRate,ed.default_value AS defaultValue FROM entity_emotions_current eec JOIN emotion_definitions ed ON ed.id=eec.emotion_id WHERE eec.entity_id=UUID_TO_BIN(?) AND ed.active=1`,[entityId]);const hours=Math.min(Math.max(Number(deltaHours)||0,0),6),appraisal=emotionAppraisal(actionType,changes,{...(appraisalContext||{}),deltaHours:hours}),result=[];for(const row of rows){const oldIntensity=round5(row.intensity),baseline=clamp(row.defaultValue),decayCoefficient=Math.max(0,Number(row.decayRate)||0)*0.5,relaxation=hours>0?1-Math.exp(-decayCoefficient*hours):0,passiveDecay=(baseline-oldIntensity)*relaxation,rawAppraisal=Number(appraisal[row.code]||0),stimulusScale=appraisalContext?.event?Math.max(0.55,1-0.35*oldIntensity):hours,next=round5(clamp(oldIntensity+passiveDecay+rawAppraisal*stimulusScale)),historyDelta=round5(next-oldIntensity);if(Math.abs(historyDelta)<0.000001)continue;const [updated]=await pool.query(`UPDATE entity_emotions_current SET intensity=?,updated_simulation_at=?,version=version+1 WHERE entity_id=UUID_TO_BIN(?) AND emotion_id=UUID_TO_BIN(?) AND version=?`,[next,simulationTime,entityId,row.emotionId,row.version||1]);if(!updated.affectedRows)continue;await pool.query(`INSERT INTO entity_emotion_history(id,entity_id,emotion_id,old_intensity,new_intensity,delta,simulation_time,cause_event_id,cause_action_id) VALUES(UUID_TO_BIN(?),UUID_TO_BIN(?),UUID_TO_BIN(?),?,?,?,?,UUID_TO_BIN(?),UUID_TO_BIN(?))`,[uuid(),entityId,row.emotionId,oldIntensity,next,historyDelta,simulationTime,causeEventId,causeActionId]);result.push({code:row.code,old:oldIntensity,new:next,delta:historyDelta});}return result;}
async function getTraits(entityId){const [rows]=await pool.query(`SELECT BIN_TO_UUID(etc.trait_id) AS traitId,td.code,etc.value,etc.version,td.volatility FROM entity_traits_current etc JOIN trait_definitions td ON td.id=etc.trait_id WHERE etc.entity_id=UUID_TO_BIN(?) AND td.active=1`,[entityId]);return rows;}
function traitBehaviorWeights(actionType){return {...(TRAIT_BEHAVIOR_LINKS[String(actionType||"").toUpperCase()]||{})};}
async function developTraits(entityId,simulationTime,evidence={},causeEventId=null,causeActionId=null){const traits=await getTraits(entityId),out=[];if(!causeActionId)return out;const actionType=String(evidence?.actionType||"").toUpperCase();if(!actionType)return out;const [actionRows]=await pool.query(`SELECT action_type AS actionType,result,started_simulation_at AS startedAt,completed_simulation_at AS completedAt,decision_id AS decisionId FROM actions WHERE entity_id=UUID_TO_BIN(?) AND status='COMPLETED' ORDER BY completed_simulation_at DESC LIMIT 32`,[entityId]);const parsedRows=actionRows.map(row=>({...row,result:parseJson(row.result,{})||{}}));const same=parsedRows.filter(row=>String(row.actionType||"").toUpperCase()===actionType).slice(0,12);if(same.length<4)return out;const outcomes=same.map(row=>String(row.result?.outcome||"SUCCESS").toUpperCase());const successRate=outcomes.filter(v=>v==="SUCCESS").length/outcomes.length;const failureRate=outcomes.filter(v=>v==="FAILURE").length/outcomes.length;const partialRate=outcomes.filter(v=>v==="PARTIAL").length/outcomes.length;const outcomeValence=successRate-failureRate-partialRate*.25;const repetition=Math.min(1,same.length/10);let socialReward=.5;const targetEntityId=evidence.targetEntityId||same[0]?.result?.targetEntityId||null;if(actionType==="TALKING"&&targetEntityId){const [relationshipRows]=await pool.query(`SELECT AVG(COALESCE(familiarity_score,0)) AS familiarity,AVG(COALESCE(closeness_score,0)) AS closeness,AVG(COALESCE(affection_score,0)) AS affection,AVG(COALESCE(trust_score,0)) AS trust FROM relationships WHERE simulation_id=(SELECT simulation_id FROM entities WHERE id=UUID_TO_BIN(?) LIMIT 1) AND status='ACTIVE' AND ((source_entity_id=UUID_TO_BIN(?) AND target_entity_id=UUID_TO_BIN(?)) OR (source_entity_id=UUID_TO_BIN(?) AND target_entity_id=UUID_TO_BIN(?)))`,[entityId,entityId,targetEntityId,targetEntityId,entityId]);const relation=relationshipRows[0]||{};socialReward=clamp((Number(relation.familiarity||0)+Number(relation.closeness||0)+Number(relation.affection||0)+Number(relation.trust||0))/4,.5);}const [entityRows]=await pool.query(`SELECT attributes FROM entities WHERE id=UUID_TO_BIN(?) LIMIT 1`,[entityId]);const attributes=parseJson(entityRows[0]?.attributes,{})||{},mentalState=attributes.mentalState&&typeof attributes.mentalState==='object'?attributes.mentalState:{};const certainty=clamp(mentalState.certainty??.5,.5),rumination=clamp(mentalState.rumination??.1,.1);const selfPerception=clamp(.75+(certainty-.5)*.45-rumination*.12,.55,1.25);const decisionEvidence=(evidence.goalId||evidence.planId||evidence.intentionId)?1:.75;const weights=traitBehaviorWeights(actionType);for(const trait of traits){const behaviorWeight=Number(weights[trait.code]||0);if(!behaviorWeight)continue;const socialFactor=actionType==="TALKING"?(0.65+socialReward*.7):1;const consistency=Math.max(-1,Math.min(1,outcomeValence));const confidenceFactor=Math.max(.55,Number(trait.volatility||.5)>0?1-Number(trait.volatility||.5)*.15:1);const delta=Math.max(-.0045,Math.min(.0045,behaviorWeight*consistency*repetition*selfPerception*decisionEvidence*socialFactor*.0015*confidenceFactor));if(Math.abs(delta)<0.000001)continue;const old=round5(trait.value),next=round5(clamp(old+delta)),historyDelta=round5(next-old);if(Math.abs(historyDelta)<0.000001)continue;const [updated]=await pool.query(`UPDATE entity_traits_current SET value=?,updated_simulation_at=?,version=version+1 WHERE entity_id=UUID_TO_BIN(?) AND trait_id=UUID_TO_BIN(?) AND version=?`,[next,simulationTime,entityId,trait.traitId,trait.version]);if(!updated.affectedRows)continue;await pool.query(`INSERT INTO entity_trait_history(id,entity_id,trait_id,old_value,new_value,changed_simulation_at,cause_event_id,cause_action_id,change_reason) VALUES(UUID_TO_BIN(?),UUID_TO_BIN(?),UUID_TO_BIN(?),?,?,?,?,UUID_TO_BIN(?),UUID_TO_BIN(?),?)`,[uuid(),entityId,trait.traitId,old,next,historyDelta,simulationTime,causeEventId,causeActionId,`contextual evidence ${actionType}: success=${successRate.toFixed(2)}, failure=${failureRate.toFixed(2)}, partial=${partialRate.toFixed(2)}, repetition=${same.length}, socialReward=${socialReward.toFixed(2)}`]);out.push({code:trait.code,old,next,delta:historyDelta,pattern:{actionType,successRate,failureRate,partialRate,count:same.length,repetition,socialReward,selfPerception}});}return out;}
module.exports={ensureEntityState,readNeeds,updateNeeds,applyEmotions,getTraits,developTraits,emotionAppraisal,traitBehaviorWeights};
